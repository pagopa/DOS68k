import { TopBar } from '@/components/top-bar'

export function AdminPage() {
  return (
    <div className="flex h-screen flex-col">
      <TopBar />
      <main className="flex flex-1 items-center justify-center text-gray-400">
        Admin — coming in slice #5
      </main>
    </div>
  )
}
